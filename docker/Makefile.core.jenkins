.PHONY: clean coldstart mrproper up
all: up

compose := docker-compose \
	-f docker-compose.lisk.core.yml \

lisk-core:
	$(compose) up --detach

ready:
	curl --silent --fail 'http://localhost:5001/api/v2/blocks' >/dev/null

mrproper: down
