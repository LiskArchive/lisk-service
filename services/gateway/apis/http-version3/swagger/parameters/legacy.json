{
    "publicKeyRequired": {
        "name": "publicKey",
        "in": "query",
        "description": "Public key of the account to query.",
        "type": "number",
        "minLength": 1,
        "maxLength": 64,
        "required": true
    }
}
