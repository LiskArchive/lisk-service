.PHONY: clean coldstart mrproper up
all: up

compose := docker-compose \
	-f docker-compose.lisk.core.yml \

lisk-core:
	$(compose) up --detach

ready:
	curl --silent --fail 'http://localhost:8988/api/node/info' >/dev/null

mrproper: down
