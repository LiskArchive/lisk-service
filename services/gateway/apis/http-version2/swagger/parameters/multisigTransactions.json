{
    "address": {
        "name": "address",
        "in": "query",
        "description": "Account address to query",
        "type": "string",
        "format": "address",
        "minLength": 1
    },
    "publicKey": {
        "name": "publicKey",
        "in": "query",
        "description": "Public key to query",
        "type": "string",
        "format": "publicKey",
        "minLength": 1
    },
    "serviceId": {
        "name": "serviceId",
        "in": "query",
        "description": "serviceId to query",
        "type": "string",
        "format": "serviceId",
        "minLength": 1
    },
    "rejectMultisigTransaction": {
        "name": "rejectMultisigTransaction",
        "in": "body",
        "schema": {
            "$ref": "#/definitions/rejectMultisigTransaction"
        }
    },
    "multisigTransaction": {
        "name": "multisigTransaction",
        "in": "body",
        "schema": {
            "$ref": "#/definitions/multisigTransaction"
        }
    },
    "patchMultisigTransaction": {
        "type": "object",
        "in": "body",
        "schema": {
            "$ref": "#/definitions/patchMultisigTransaction"
        }
    }
}
